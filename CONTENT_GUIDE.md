# Content Guide

This file explains how to add more content to movies and series pages.

## Where content is stored now

- `website/movies.html` -> `MOVIES` array inside the React script
- `website/series.html` -> `SERIES` array inside the React script
- `docs/` is deployment copy (generated by syncing from `website/`)

## Add a new movie

Open `website/movies.html` and add a new object in `MOVIES`:

```js
{
  title: "Movie Title",
  year: 2024,
  director: "Director Name",
  genre: "Drame Psychologique",
  image: "https://image-url-or-local-path"
}
```

Valid genres currently used:

- `Nouvelle Vague`
- `Drame Psychologique`
- `Polar Français`
- `Comédie Dramatique`

## Add a new series

Open `website/series.html` and add a new object in `SERIES`:

```js
{
  title: "Series Title",
  genre: "Drame",
  year: 2024,
  episodes: 8,
  image: "https://image-url-or-local-path"
}
```

Valid genres currently used:

- `Drame`
- `Polar`
- `Histoire`
- `Comédie`

## Pagination behavior

- Movies page: `6` items per page
- Series page: `4` items per page
- Pagination updates automatically after filtering/search.

## Publish updated content

1. Update content in `website/`.
2. Sync deployment copy:
   - Windows: `robocopy website docs /E`
3. Commit and push.
